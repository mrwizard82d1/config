# Configuration for Git (for work?)

This file contains the configuration information that I use for Git at
work.

It contains my .gitconfig file (view it using `ls -la`.) and my ignore
files in `git/ignore`. (To "install" the ignored files, excecute the
command `cp -R git ~/.config/`. [You may need to create the `.config`
file first.]. The final configuration directory structure should be
`~/.config/git/ignore`.)

